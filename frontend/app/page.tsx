import { LazyHomePageWrapper } from '@/components/ui/lazy-home-page';

// This is now a proper Next.js page component with lazy loading
export default function HomePage() {
  return <LazyHomePageWrapper />;
}
